### Prepare your S3 bucket

Make sure:

- Bucket is created with default settings

- **Static website hosting** is **disabled**, we'll serve via **CloudFront with HTTPS**

- Block all public access ✅

- Bucket policy customized to allow CloudFront access only (Follow the example below)

Example bucket policy:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "PublicReadGetObject",
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::YOUR-BUCKET-NAME/*"
    },
    {
      "Sid": "AllowCloudFrontServicePrincipal",
      "Effect": "Allow",
      "Principal": {
        "Service": "cloudfront.amazonaws.com"
      },
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::YOUR-BUCKET-NAME/*",
      "Condition": {
        "ArnLike": {
          "AWS:SourceArn": "arn:aws:cloudfront::YOUR-AWS-ACCOUNT-ID:distribution/YOUR-CLOUDFRONT-DISTRIBUTION-ID"
        }
      }
    }
  ]
}
```

AWS S3 bucket, settings and policy read to go! 🚀

Jump to the [CloudFront setup guide](./CloudFront.md) to continue.
